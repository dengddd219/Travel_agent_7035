# 本地启动说明

## 每次启动步骤

打开终端（PowerShell 或 CMD），进入项目根目录，然后依次执行：

```bash
# 1. 进入项目目录
cd C:\Users\19841\Desktop\github\7035project\Travel_agent_7035

# 2. 激活虚拟环境
.venv\Scripts\activate

# 3. 启动服务器
uvicorn backend.server:app --reload
```

启动成功后终端会显示：
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

然后浏览器打开 **http://localhost:8000/** 即可使用。

---

## 停止服务器

在终端按 `Ctrl + C`。

---

## 注意事项

- **不要关闭终端窗口**，关掉终端服务器就停了
- 每次改了 Python 代码，`--reload` 会自动重载，不用手动重启
- 改了 `.env` 文件需要手动重启（Ctrl+C 再执行步骤 3）
- 前端入口是 `/`，不需要加 `/chat`

---

## 环境变量

首次运行前确保项目根目录有 `.env` 文件，参考 `.env.example` 填写：

```
FOUNDRY_PROJECT_API_KEY=...
FOUNDRY_PROJECT_ENDPOINT=...
FOUNDRY_PROJECT_DEPLOYMENT=...
AMAP_API_KEY=...
```
