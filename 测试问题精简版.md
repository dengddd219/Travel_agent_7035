# 联调测试精简版（沟通稿）

> 说明：本文档用于组内快速同步测试现象与后续优化方向，重点聚焦可落地的改进点。

## 第一部分：测试内容与实际回答

### 测试 1

- 提问：我和朋友要去厦门玩五天，给我规划一个路线，五一假期出行。
- 回答要点（节选）：
  - 标题为 `Xiamen Intelligent Travel Plan`
  - 行程摘要显示 `A 3-day leisure itinerary in Xiamen`
  - 输出了 Day 1-3（未覆盖 5 天）
  - Review 中出现：`must_visit_coverage`，提示未覆盖 `给我规划一个路线 五一假期出行`

### 测试 2

- 提问：我和朋友要去厦门玩五天，给我规划一个路线，从深圳出发，五一假期出行。
- 回答要点（节选）：
  - 标题为 `Shenzhen Intelligent Travel Plan`
  - 行程摘要显示 `A 3-day leisure itinerary in Shenzhen`
  - 输出了 Day 1-3（未覆盖 5 天）
  - Day 3 出现“Candidate POIs were exhausted early”

---

## 第二部分：待优化问题与建议修改模块

### 1) 关键信息抽取有待优化（建议优先）

- 现象：`给我规划路线`、`五一出行` 这类指令/时间描述被当成 must-visit。
- 影响：会触发不必要的告警，干扰后续重规划效果。
- 建议修改模块：
  - `3-travel_planner/travel_planner/agent.py`
  - 重点关注：`_extract_clause`、must_visit 回填逻辑、文本清洗规则

### 2) 评审与重规划闭环可再增强

- 现象：系统显示已 review/replan，但关键约束（目的地、天数）仍可能未满足。
- 影响：质量闸门通过了流程，但没有充分拦住关键偏差。
- 建议修改模块：
  - `3-travel_planner/travel_planner/planner.py`
  - 建议补充“硬约束校验”：目的地一致、trip_days 一致、must_visit 为实体地点

### 3) 出发地与目的地语义可进一步区分

- 现象：`从深圳出发去厦门` 的句式中，存在将“深圳”误判为目的地的情况。
- 影响：行程主城市可能偏移。
- 建议修改模块：
  - `3-travel_planner/travel_planner/agent.py`
  - 建议新增/强化：`origin_city` 与 `destination_city` 双槽位解析

### 4) 天数约束建议强化绑定

- 现象：用户提 5 天，结果仍偏向 3 天模板。
- 影响：输出与用户预期不一致。
- 建议修改模块：
  - `3-travel_planner/travel_planner/agent.py`（用户画像抽取）
  - `3-travel_planner/travel_planner/planner.py`（计划生成与校验）

### 5) 输出语言风格建议统一

- 现象：用户中文提问时，回答中仍有较多英文段落标题（如 Planning Summary、Review Summary）。
- 影响：阅读体验有割裂感，沟通风格不够统一。
- 建议修改模块：
  - `3-travel_planner/travel_planner/planner.py`（报告模板文案）
  - `3-travel_planner/travel_planner/ui_backend.py`（前端返回文本组织）

### 6) 日期锚点建议进一步稳定

- 现象：用户提到“五一出行”时，天气和行程窗口有时仍按“当前日期附近”生成。
- 影响：节假日语义没有稳定落到实际计划周期，影响可执行性与可信度。
- 建议修改模块：
  - `3-travel_planner/travel_planner/agent.py`（日期/节假日解析）
  - `3-travel_planner/travel_planner/tools/weather_adapter.py`（天气窗口对齐）
  - `3-travel_planner/travel_planner/planner.py`（计划时间窗绑定）

### 7) 候选点不足导致空白天

- 现象：出现 “Candidate POIs were exhausted early” 时，某天内容明显偏空。
- 影响：多天行程完整性下降，用户会感觉“路线没排满”。
- 建议修改模块：
  - `3-travel_planner/travel_planner/tools/strategy_rag_adapter.py`（召回候选扩充）
  - `3-travel_planner/travel_planner/tools/poi.py`（POI 补点与去重）
  - `3-travel_planner/travel_planner/planner.py`（日程兜底填充规则）

---

## 建议的轻量回归标准

1. 输入“去厦门 5 天，从深圳出发，五一出行”时，输出主城市为厦门、天数为 5。
2. must-visit 不包含指令句和时间句。
3. 若目的地或天数不一致，review 应给出明确失败并触发有效 replan。
4. 中文输入场景下，报告主体默认使用中文（专有名词可保留英文）。
5. 含节假日表达（如“五一”）时，天气与行程日期窗口应与节假日一致，且不出现“POI exhausted early”导致的空白天。
